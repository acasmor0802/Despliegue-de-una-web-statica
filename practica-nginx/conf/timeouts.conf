# Configuración de timeouts personalizada
# keepalive_timeout se debe configurar en el contexto http (nginx.conf)
# o en el bloque server (default.conf)
# Este archivo está reservado para futuras configuraciones de timeout
